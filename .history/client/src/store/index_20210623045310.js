import { createStore } from "vuex";

export default createStore({
    state() {

    },
    mutations: {
        setUser(state) {
            state.user = user,
                state.token = token,
        }
    },
});